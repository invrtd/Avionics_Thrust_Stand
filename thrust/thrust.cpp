#include "thrust.h"
#include "../config.h"
#include <Arduino.h>
 
void thrust_init() {
  // Initialize thrust measurement (e.g., set pin modes, start ADC, etc.)
    Serial.println("Thrust measurement initialized.");
}
 
 
